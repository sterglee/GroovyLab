

A = M("1 1 1;2 3 1; 1 -1 -2")
b = M("2; 3; -6")

x = A / b  // the "/" operator works for solve

x2 = solve(A, b)
A*x-b


Ainv = inv(A)  // computing the inverse
Aejml = EJMLinv(A)
I3 = eye(3,3)
residual = Ainv*A-I3  // should be zero


